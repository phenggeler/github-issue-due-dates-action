name: Mark Tasks Overdue
on: [push]
jobs:
  Ubuntu:
    name: Add labels to overdue issues
    runs-on: ubuntu-latest
    steps:
      - name: GitHub Issue Due Dates Action
        uses: phenggeler/github-issue-due-dates-action@1.1.0
        with:
          GH_TOKEN: "${{ secrets.GH_TOKEN }}"
